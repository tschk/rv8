interface:
  display_name: "Rv8"
  short_description: "Repo-specific patterns and workflows for rv8"
  default_prompt: "Use the rv8 repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true